services:
  - type: web
    name: my-vindhu
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18
